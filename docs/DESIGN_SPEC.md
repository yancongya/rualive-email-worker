# 🎨 RuAlive 设计与开发规范文档 (Design System & Dev Spec)

## 1. 核心设计理念 (Core Concept)
*   **品牌格调**：黑色幽默、动画师共鸣、极简但富有动感、"工作搭子"感。
*   **视觉特征**：极深背景 (#050505)、高对比度橙色 (#FF6B35)、玻璃拟态、大尺度标题、流畅的 GSAP 动效。
*   **文案调性**：反讽、鼓励、诙谐、中英双语（全大写英文增强视觉张力）。

---

## 2. 视觉规范 (Visual Specs)

### 2.1 色彩系统 (Color Palette)
| 用途 | 颜色名称 | 颜色值 | 说明 |
| :--- | :--- | :--- | :--- |
| **背景主色** | `dark` | `#050505` | 绝对深色，用于页面底层 |
| **卡片/浮层** | `dark-surface` | `rgba(255,255,255,0.03)` | 配合 `backdrop-blur-md` 实现玻璃感 |
| **品牌主色** | `primary` | `#ff6b35` | 充满活力的橙色，用于 CTA 和重要强调 |
| **主色-亮** | `primary-light` | `#ff8c42` | 用于悬停（Hover）状态 |
| **辅助文字** | `text-dim` | `rgba(255,255,255,0.4)` | 用于副标题和说明文案 |
| **边框** | `border-dim` | `rgba(255,255,255,0.1)` | 极细边框，维持精致感 |

### 2.2 字体排版 (Typography)
*   **英文字体**：`Plus Jakarta Sans` (优先使用 800/Black 字重，并开启 `italic`)。
*   **中文字体**：`Noto Sans SC` (900/Black 字重用于标题)。
*   **标题规则**：使用 `italic` (斜体) 和 `uppercase` (全大写)，`tracking-tighter` (紧凑字间距)。

---

## 3. 技术栈规范 (Tech Stack)
*   **框架**：React 19。
*   **样式**：Tailwind CSS。
*   **动画核心**：GSAP (Tweening, Timeline, Observer, Draggable)。
*   **图标库**：Inline SVG。

---

## 4. 核心组件规范 (UI Components)

### 4.1 玻璃卡片 (Glassmorphism Card)
*   **样式**：`bg-white/5` + `backdrop-blur-xl` + `border-white/10` + `rounded-[2rem]`。

### 4.2 交互按钮 (The Pulse Button)
*   **主按钮**：`bg-primary`, `text-white`, `font-black`, `italic`, `rounded-2xl`。
*   **交互**：点击时 `active:scale-95`。

### 4.3 输入组件 (Input Fields)
*   **风格**：`bg-white/5`, `border-white/10`, `focus:border-primary`。

---

## 5. 动画与交互规范 (Motion & UX)

### 5.1 页面切换 (The View-Switcher)
*   **转场**：使用 `gsap.to(opacity: 0)` -> 修改状态 -> `gsap.to(opacity: 1)`。
*   **重置**：回到首页时重置 `currentSection` 为 0 并复位 `wrapper` 的 `y` 位移。

### 5.2 装饰动效 (Aesthetic Motion)
*   **心跳感**：重要数字或 Logo 增加 `animate-pulse`。
*   **彩蛋**：保留随机 Slogan 弹出。