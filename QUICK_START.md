# RuAlive Email Worker - 快速开始

## 🚀 一键部署

**双击运行 `deploy.bat` 即可完成所有部署步骤！**

## 📁 目录说明

```
rualive-email-worker/
├── deploy.bat              ⭐ 一键构建部署（推荐使用）
├── build.bat               ⭐ 仅构建仪表板
├── src/                    源代码
├── scripts/                构建和部署脚本
├── tests/                  测试脚本
├── docs/                   文档
└── *.sql                   数据库结构文件
```

## 🛠️ 常用操作

### 修改代码后重新部署

1. 修改 `src/` 目录下的任何文件
2. 双击运行 `deploy.bat`
3. 等待部署完成

### 仅构建不部署

如果只想构建仪表板但不部署，运行 `build.bat`

### 分步操作

如果需要分步操作：

1. **构建**: `node scripts\build-dashboard.js`
2. **上传 KV**: `scripts\upload-kv.bat`
3. **部署**: `scripts\deploy-worker.bat`

## 🌐 访问地址

- **首页**: https://rualive-email-worker.cubetan57.workers.dev/
- **用户面板**: https://rualive-email-worker.cubetan57.workers.dev/user
- **管理后台**: https://rualive-email-worker.cubetan57.workers.dev/admin

## 📖 详细文档

查看 `docs/PROJECT_STRUCTURE.md` 了解完整的项目结构和详细说明。

## ⚠️ 注意事项

- 所有文件必须使用 UTF-8 编码
- 修改 HTML/CSS/JS 组件后必须重新构建
- 首次使用需要初始化数据库（执行 schema.sql）

## 🔧 故障排除

### 部署失败
- 检查是否已登录 Cloudflare: `npx wrangler whoami`
- 检查网络连接

### 页面加载失败
- 检查浏览器控制台错误信息
- 确认 KV 存储中有 `user-dashboard-inline` 键

### 查看实时日志
```bash
npx wrangler tail
```